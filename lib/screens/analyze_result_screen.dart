import 'package:flutter/material.dart';

class AnalyzeResultsScreen extends StatelessWidget {
  const AnalyzeResultsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("Pick the appropriate result"),
      ),
    );
  }
}
